%%%-------------------------------------------------------------------
%%% @doc 测试桩：导出 init/1 但不实现任何 around_* 回调。
%%% 用于验证 beamai_middleware_runner 跳过无钩子的中间件。
%%% @end
%%%-------------------------------------------------------------------
-module(mw_stub_noop).

-export([init/1]).

init(Opts) -> Opts#{stub => noop}.
